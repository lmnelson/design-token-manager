# Figma API (optional - for Figma integration)
FIGMA_ACCESS_TOKEN=

# Database (Neon PostgreSQL)
DATABASE_URL=                 # Pooled connection string (runtime)
DATABASE_URL_UNPOOLED=        # Direct/unpooled connection (migrations)

# Auth0 (v4 SDK)
AUTH0_DOMAIN=                 # your-tenant.auth0.com (without https://)
AUTH0_CLIENT_ID=
AUTH0_CLIENT_SECRET=
AUTH0_SECRET=                 # Generate with: openssl rand -hex 32
APP_BASE_URL=http://localhost:3000
